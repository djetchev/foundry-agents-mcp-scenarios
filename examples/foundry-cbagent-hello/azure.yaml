# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: foundry-cbagent-hello
metadata:
  template: foundry-cbagent-hello

infra:
  provider: bicep
  path: ./infra

pipeline:
  provider: github

services:
  agent:
    project: .
    language: py
    host: containerapp
